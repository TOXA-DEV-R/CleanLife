if (document.querySelector(".my-slider")) {
  var slider = tns({
    container: ".my-slider",
    items: 3,
    slideBy: "page",
    autoplay: true,
  });
}

// Dropdown menu

const menuInitItem = document.querySelectorAll(".menu-init__item");
